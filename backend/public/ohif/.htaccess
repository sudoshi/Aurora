<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /ohif/
    # If the file/directory doesn't exist, fall back to index.html (SPA routing)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /ohif/index.html [L]
</IfModule>
